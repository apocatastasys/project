image: gitlab/dind

stages:
  - build
  - push
 # - deploy

before_script:
  - whoami
  - docker login -u apocatastasys -p 3c150b4e-abf8-430e-904e-176cda69a3f3
build_job:
  stage: build
  script:
    #- docker run  -d --name mymongo -p 27017:27017 mongo
    #- docker push 
    - docker build ./rabbitmq/ --tag apocatastasys/rabbitmq
    #- docker login -u apocatastasys -p 3c150b4e-abf8-430e-904e-176cda69a3f3
    
    #- docker run -d --name rabbitmq1.0 rabbitmq 
    #- docker build ./search_engine_crawler/ -t crawler:1.0
    #- docker run -d --name Crawler crawler:1.0
    #- docker build ./search_engine_ui/ -t app:1.0
    #- docker run --name App1.0 -p 8000:8000 app:1.0
  tags:
    - docker 

push-image:
  stage: push
  script:
    - docker push apocatastasys/rabbitmq
  tags: 
    - docker

